# 随机过程课后题第四章答案
## 4

### 第一步：证明 $\{X(t)\}$ 是平稳过程（宽平稳）

要证明一个过程是宽平稳的，我们需要验证两个条件：
1.  **均值函数** $E[X(t)]$ 是常数。
2.  **自相关函数** $R_X(t_1, t_2)$ 只与时间差 $\tau = t_1 - t_2$ 有关。

已知 $\alpha \sim U(0, 1)$，其概率密度函数为：
$$
f(\alpha) = \begin{cases} 1, & 0 < \alpha < 1 \\ 0, & \text{其他} \end{cases}
$$

#### 1. 验证均值
计算 $X(t)$ 的数学期望：
$$
E[X(t)] = \int_{-\infty}^{+\infty} x f(x) dx = \int_{0}^{1} \sin(2\pi \alpha t) \cdot 1 \, d\alpha
$$
由于 $t$ 是整数 ($t=1, 2, \dots$)，积分结果为：
$$
E[X(t)] = \left[ -\frac{\cos(2\pi \alpha t)}{2\pi t} \right]_{0}^{1} = -\frac{1}{2\pi t} (\cos(2\pi t) - \cos(0))
$$
因为 $t$ 是整数，$\cos(2\pi t) = 1$，且 $\cos(0)=1$，所以：
$$
E[X(t)] = -\frac{1}{2\pi t} (1 - 1) = 0
$$
**结论：** 均值为常数 0，**满足条件 1**。

#### 2. 验证自相关函数
计算 $R_X(t, t+\tau)$，令 $t_1 = t, t_2 = t+\tau$：
$$
R_X(t, t+\tau) = E[X(t)X(t+\tau)] = E[\sin(2\pi \alpha t) \sin(2\pi \alpha (t+\tau))]
$$
利用积化和差公式 $\sin A \sin B = \frac{1}{2}[\cos(A-B) - \cos(A+B)]$：
$$
R_X(t, t+\tau) = \frac{1}{2} E[\cos(2\pi \alpha \tau) - \cos(2\pi \alpha (2t+\tau))]
$$
利用期望的线性性质分开计算：
$$
= \frac{1}{2} E[\cos(2\pi \alpha \tau)] - \frac{1}{2} E[\cos(2\pi \alpha (2t+\tau))]
$$

我们来分别分析这两个期望值的积分：
* **第一项** $E[\cos(2\pi \alpha \tau)]$：
$$
\int_{0}^{1} \cos(2\pi \alpha \tau) d\alpha
$$
* 当 $\tau = 0$ 时，$\int_{0}^{1} 1 \, d\alpha = 1$。
* 当 $\tau \neq 0$ (且为整数) 时，$\left[ \frac{\sin(2\pi \alpha \tau)}{2\pi \tau} \right]_0^1 = 0$。
* **第二项** $E[\cos(2\pi \alpha (2t+\tau))]$：
由于 $t \ge 1$，系数 $(2t+\tau)$ 是非零整数（除非极特殊情况如 $\tau=-2t$，但在自相关定义中我们通常关注平稳性结构，且即使非零整数，在 $0$ 到 $1$ 的完整周期上积分也是 0）。
$$
\int_{0}^{1} \cos(2\pi \alpha (2t+\tau)) d\alpha = 0 \quad (\text{因为是在完整周期上的积分})
$$

因此，自相关函数为：
$$
R_X(\tau) = \begin{cases} 1/2, & \tau = 0 \\ 0, & \tau \neq 0 \end{cases}
$$
**结论：** 自相关函数只与时间差 $\tau$ 有关，与具体时刻 $t$ 无关，**满足条件 2**。

**综上所述，$\{X(t)\}$ 是宽平稳过程。**

---

### 第二步：证明 $\{X(t)\}$ 不是严平稳过程

**严平稳的定义**要求：对于任意 $n$ 和时间位移 $k$，联合分布 $(X(t_1), \dots, X(t_n))$ 必须与 $(X(t_1+k), \dots, X(t_n+k))$ 完全相同。

要证明**不是**严平稳，只需要找到一个反例，说明其联合分布随时间平移发生了变化。

我们考察 $t=1$ 和 $t=2$ 时刻的随机变量之间的**依赖关系**。

1.  **观察 $X(1)$ 和 $X(2)$ 的关系：**
$$X(1) = \sin(2\pi \alpha)$$
$$X(2) = \sin(4\pi \alpha) = 2\sin(2\pi \alpha)\cos(2\pi \alpha)$$
这里存在一个确定的函数关系（尽管有符号模糊性，但二者强相关）。具体来说，如果不考虑符号，$\left|X(2)\right| = 2\left|X(1)\right|\sqrt{1-X(1)^2}$。

2.  **观察平移后的 $X(2)$ 和 $X(3)$ 的关系：**
如果我们把时间平移 1 个单位，原来的 $(X(1), X(2))$ 变成了 $(X(2), X(3))$。
$$X(2) = \sin(4\pi \alpha)$$
$$X(3) = \sin(6\pi \alpha)$$
利用三倍角公式思想，$\sin(6\pi \alpha)$ 不能用 $\sin(4\pi \alpha)$ 以类似于 "$2x\sqrt{1-x^2}$" 的形式简单表示。

更直观地说，$X(1)$ 是基波，$X(2)$ 是二次谐波。$X(1)$ 和 $X(2)$ 之间的函数约束关系（由二倍角公式决定），与 $X(2)$ 和 $X(3)$ 之间的函数约束关系（由 $2\omega$ 和 $3\omega$ 的关系决定）是**不一样的**。

**结论：**
由于 $(X(1), X(2))$ 的联合概率分布（由二倍角关系约束）与 $(X(2), X(3))$ 的联合概率分布（由 2:3 频率关系约束）结构不同。
即：
$$
F_{X(1), X(2)}(x_1, x_2) \neq F_{X(2), X(3)}(x_1, x_2)
$$
不满足位移不变性。

**因此，$\{X(t)\}$ 不是严平稳过程。**

---

## 6


### 详细证明步骤（考试规范写法）

#### 第一步：说明 $\{X(t)\}$ 服从正态分布
由于 $\{Y(t), t \ge 0\}$ 是正态过程，而 $X(t) = Y(t+b) - Y(t)$ 是 $Y(t)$ 的线性组合（线性变换），根据正态过程的性质，$\{X(t), t \ge 0\}$ 也是一个**正态过程**。

> **注**：这一点至关重要，是后续由“宽”推“严”的基石。

#### 第二步：证明 $\{X(t)\}$ 是宽平稳过程
我们需要验证 $X(t)$ 的均值函数为常数，且协方差函数只与时间差 $\tau$ 有关。

**1. 计算均值函数 $m_X(t)$**
已知 $m_Y(t) = \alpha + \beta t$，则：
$$
\begin{aligned}
m_X(t) &= E[X(t)] \\
&= E[Y(t+b) - Y(t)] \\
&= E[Y(t+b)] - E[Y(t)] \\
&= [\alpha + \beta(t+b)] - [\alpha + \beta t] \\
&= \alpha + \beta t + \beta b - \alpha - \beta t \\
&= \beta b
\end{aligned}
$$
**结论：** $m_X(t) = \beta b$ 是一个常数（与 $t$ 无关）。

**2. 计算自协方差函数 $C_X(t, t+\tau)$**
根据定义，$C_X(t, t+\tau) = \text{Cov}(X(t), X(t+\tau))$。
代入 $X(t)$ 的表达式：
$$
C_X(t, t+\tau) = \text{Cov}(Y(t+b) - Y(t), \ Y(t+\tau+b) - Y(t+\tau))
$$
利用协方差的双线性性质 $\text{Cov}(A-B, C-D) = \text{Cov}(A,C) - \text{Cov}(A,D) - \text{Cov}(B,C) + \text{Cov}(B,D)$，我们将上式展开为四项。

已知 $C_Y(t_1, t_2) = e^{-\alpha |t_1 - t_2|}$，即只取决于两点的时间差。

* **第一项**：$\text{Cov}(Y(t+b), Y(t+\tau+b))$
时间差为 $(t+\tau+b) - (t+b) = \tau$。
$\implies e^{-\alpha |\tau|}$
* **第二项**：$-\text{Cov}(Y(t+b), Y(t+\tau))$
时间差为 $(t+\tau) - (t+b) = \tau - b$。
$\implies -e^{-\alpha |\tau - b|}$
* **第三项**：$-\text{Cov}(Y(t), Y(t+\tau+b))$
时间差为 $(t+\tau+b) - t = \tau + b$。
$\implies -e^{-\alpha |\tau + b|}$
* **第四项**：$\text{Cov}(Y(t), Y(t+\tau))$
时间差为 $(t+\tau) - t = \tau$。
$\implies e^{-\alpha |\tau|}$

将四项合并：
$$
C_X(t, t+\tau) = 2e^{-\alpha |\tau|} - e^{-\alpha |\tau - b|} - e^{-\alpha |\tau + b|}
$$
**结论：** 上式结果只包含 $\tau$ 和常数 $\alpha, b$，与具体时刻 $t$ 无关。记为 $C_X(\tau)$。

**综上，$\{X(t)\}$ 是宽平稳过程。**

#### 第三步：得出严平稳结论
由于 $\{X(t)\}$ 是**正态过程**，且已证明它是**宽平稳**的。
根据正态过程的性质：**正态过程的宽平稳性与严平稳性是等价的**。
因此，$\{X(t), t \ge 0\}$ 是严平稳过程。

---

## 7

解答这道题需要验证三个条件。

**联合平稳（Jointly Wide-Sense Stationary）的定义**：
如果两个随机过程 $\{X(t)\}$ 和 $\{Y(t)\}$ 满足以下三个条件，则称它们是联合平稳的：
1.  $\{X(t)\}$ 是宽平稳过程。
2.  $\{Y(t)\}$ 是宽平稳过程。
3.  互相关函数 $R_{XY}(t, t+\tau) = E[X(t)Y(t+\tau)]$ 只与时间差 $\tau$ 有关，而与具体时刻 $t$ 无关。

我们依次进行证明。

---

### 第一步：证明 $\{X(t)\}$ 是平稳过程

1.  **均值**：
$$
E[X(t)] = E[X \cos(\alpha t) + Y \sin(\alpha t)] = E[X]\cos(\alpha t) + E[Y]\sin(\alpha t)
$$
已知 $E[X]=0, E[Y]=0$，所以：
$$E[X(t)] = 0 \quad (\text{常数})$$

2.  **自相关函数** $R_X(t, t+\tau)$：
$$
R_X(t, t+\tau) = E[X(t)X(t+\tau)]
$$
代入表达式：
$$
= E\Big[ (X \cos(\alpha t) + Y \sin(\alpha t)) \cdot (X \cos(\alpha(t+\tau)) + Y \sin(\alpha(t+\tau))) \Big]
$$
展开利用 $E[XY]=0$（不相关且均值为0）以及 $E[X^2]=E[Y^2]=\sigma^2$：
$$
= \sigma^2 \cos(\alpha t)\cos(\alpha(t+\tau)) + \sigma^2 \sin(\alpha t)\sin(\alpha(t+\tau))
$$
利用三角恒等式 $\cos A \cos B + \sin A \sin B = \cos(A-B)$：
$$
= \sigma^2 \cos(\alpha t - \alpha(t+\tau)) = \sigma^2 \cos(-\alpha \tau) = \sigma^2 \cos(\alpha \tau)
$$
**结论：** $R_X$ 只与 $\tau$ 有关，故 $\{X(t)\}$ 是平稳过程。

---

### 第二步：证明 $\{Y(t)\}$ 是平稳过程

1.  **均值**：
同理，$E[Y(t)] = E[Y]\cos(\alpha t) + E[X]\sin(\alpha t) = 0$。

2.  **自相关函数** $R_Y(t, t+\tau)$：
$$
R_Y(t, t+\tau) = E[Y(t)Y(t+\tau)]
$$
展开结构与 $X(t)$ 完全一致（只是系数 $X$ 和 $Y$ 交换了位置，但它们的方差相同）：
$$
= \sigma^2 \cos(\alpha t)\cos(\alpha(t+\tau)) + \sigma^2 \sin(\alpha t)\sin(\alpha(t+\tau))
$$
$$
= \sigma^2 \cos(\alpha \tau)
$$
**结论：** $R_Y$ 只与 $\tau$ 有关，故 $\{Y(t)\}$ 是平稳过程。

---

### 第三步：计算互相关函数 $R_{XY}(t, t+\tau)$

**注意：** 这里是这道题的关键点。如果题目完全按照你给出的公式，**这步会出现问题**。请仔细看推导：

$$
R_{XY}(t, t+\tau) = E[X(t)Y(t+\tau)]
$$
代入 $X(t)$ 和 $Y(t)$：
$$
= E\Big[ (X \cos(\alpha t) + Y \sin(\alpha t)) \cdot (Y \cos(\alpha(t+\tau)) + X \sin(\alpha(t+\tau))) \Big]
$$
展开括号（利用 $E[X^2]=\sigma^2, E[Y^2]=\sigma^2, E[XY]=0$）：
1.  $X \cdot Y$ 项：均值为 0。
2.  $X \cdot X$ 项：$E[X^2] \cos(\alpha t) \sin(\alpha(t+\tau)) = \sigma^2 \cos(\alpha t) \sin(\alpha(t+\tau))$
3.  $Y \cdot Y$ 项：$E[Y^2] \sin(\alpha t) \cos(\alpha(t+\tau)) = \sigma^2 \sin(\alpha t) \cos(\alpha(t+\tau))$
4.  $Y \cdot X$ 项：均值为 0。

合并结果：
$$
R_{XY}(t, t+\tau) = \sigma^2 \Big[ \sin(\alpha(t+\tau))\cos(\alpha t) + \cos(\alpha(t+\tau))\sin(\alpha t) \Big]
$$
利用三角恒等式 $\sin A \cos B + \cos A \sin B = \sin(A+B)$：
$$
= \sigma^2 \sin(\alpha(t+\tau) + \alpha t) = \sigma^2 \sin(2\alpha t + \alpha \tau)
$$

**分析结果：**
你会发现最终结果包含 $t$（即 $\sin(2\alpha t + \dots)$）。根据定义，如果互相关函数依赖于 $t$，它们**不是**联合平稳的。

---

### 💡 题目可能的勘误（考试必看）

这道题在经典的随机过程教材中非常常见，但通常题目里的 $Y(t)$ 会有一个**负号**。请检查你的题目抄写，或者出题老师是否写错了符号。

**情况 A：如果题目必须按原样做**
结论是：它们**不是**联合平稳过程，因为 $R_{XY}$ 与 $t$ 有关。

**情况 B：如果题目是 $Y(t) = Y \cos(\alpha t) - X \sin(\alpha t)$**
（注意 $X$ 前面变成了负号）
我们重新计算第三步：
$$
R_{XY} = \sigma^2 \Big[ \cos(\alpha t)(-\sin(\alpha(t+\tau))) + \sin(\alpha t)\cos(\alpha(t+\tau)) \Big]
$$
$$
= \sigma^2 \Big[ \sin(\alpha t)\cos(\alpha(t+\tau)) - \cos(\alpha t)\sin(\alpha(t+\tau)) \Big]
$$
利用公式 $\sin A \cos B - \cos A \sin B = \sin(A-B)$：
$$
= \sigma^2 \sin(\alpha t - \alpha(t+\tau)) = \sigma^2 \sin(-\alpha \tau)
$$
此时结果**与 $t$ 无关**。
只有在这种情况下，才能得出“是联合平稳过程”的结论。

## 12 

### 第一步：准备工作（计算统计平均）

在讨论各态历经之前，我们要先确认它是平稳过程，并算出它的“统计平均（Ensemble Average）”，作为后续比较的**标准答案**。

1.  **统计均值** $m_X$：
$$E[X(t)] = E[A]\cos(\omega t) + E[B]\sin(\omega t) = 0$$
2.  **统计自相关函数** $R_X(\tau)$：
$$
\begin{aligned}
R_X(t, t+\tau) &= E[X(t)X(t+\tau)] \\
&= E[(A\cos(\omega t) + B\sin(\omega t))(A\cos(\omega(t+\tau)) + B\sin(\omega(t+\tau)))]
\end{aligned}
$$
利用 $E[A^2]=E[B^2]=\sigma^2$ 和 $E[AB]=0$（独立且均值为0）：
$$
= \sigma^2 [\cos(\omega t)\cos(\omega(t+\tau)) + \sin(\omega t)\sin(\omega(t+\tau))]
$$
$$
= \sigma^2 \cos(\omega \tau)
$$
**(注：这是我们接下来要对比的“标准答案”)**

---

### 第二步：讨论均值的各态历经性

**定义**：如果一个过程的**时间平均（Time Average）**等于**统计平均**，则称其具有均值各态历经性。

设 $x(t)$ 是 $X(t)$ 的任意一个样本函数（即把 $A, B$ 看作具体的常数 $a, b$）。
计算时间平均：
$$
\begin{aligned}
\langle x(t) \rangle &= \lim_{T \to \infty} \frac{1}{2T} \int_{-T}^{T} x(t) dt \\
&= \lim_{T \to \infty} \frac{1}{2T} \int_{-T}^{T} [a\cos(\omega t) + b\sin(\omega t)] dt
\end{aligned}
$$
由于 $\cos(\omega t)$ 和 $\sin(\omega t)$ 都是有界周期函数，在无穷区间上的积分虽然振荡，但除以一个趋于无穷大的 $2T$，极限必为 **0**。

**比较：**
$$
\text{时间平均 } \langle x(t) \rangle = 0
$$
$$
\text{统计平均 } E[X(t)] = 0
$$
两者相等。
**结论：该过程具有均值各态历经性。**

---

### 第三步：讨论自相关函数的各态历经性

**定义**：如果**时间平均自相关函数**等于**统计自相关函数**，则称其具有自相关各态历经性。

计算样本函数的时间自相关 $\mathcal{R}_X(\tau)$：
$$
\mathcal{R}_X(\tau) = \langle x(t)x(t+\tau) \rangle = \lim_{T \to \infty} \frac{1}{2T} \int_{-T}^{T} x(t)x(t+\tau) dt
$$
先把被积函数展开：
$$
\begin{aligned}
x(t)x(t+\tau) &= [a\cos(\omega t) + b\sin(\omega t)] \cdot [a\cos(\omega(t+\tau)) + b\sin(\omega(t+\tau))] \\
&= a^2 \cos(\omega t)\cos(\omega(t+\tau)) + b^2 \sin(\omega t)\sin(\omega(t+\tau)) + ab[\dots]
\end{aligned}
$$
这里有一个计算技巧：利用积化和差。我们知道时间平均 $\langle \cdot \rangle$ 具有以下性质：
* $\langle \cos(\omega t)\cos(\omega(t+\tau)) \rangle = \frac{1}{2}\cos(\omega \tau)$
* $\langle \sin(\omega t)\sin(\omega(t+\tau)) \rangle = \frac{1}{2}\cos(\omega \tau)$
* $\langle \text{交叉项} \rangle = \langle \sin(2\omega t + \dots) \rangle = 0$

所以，时间平均自相关函数为：
$$
\mathcal{R}_X(\tau) = a^2 \cdot \frac{1}{2}\cos(\omega \tau) + b^2 \cdot \frac{1}{2}\cos(\omega \tau) = \frac{a^2 + b^2}{2} \cos(\omega \tau)
$$

**关键比较：**
* **时间平均**：$\mathcal{R}_X(\tau) = \frac{A^2 + B^2}{2} \cos(\omega \tau)$ （注意这里 $A, B$ 是随机变量）
* **统计平均**：$R_X(\tau) = \sigma^2 \cos(\omega \tau)$

**分析：**
要让它们相等，必须满足 $\frac{A^2 + B^2}{2} = \sigma^2$ 恒成立。
但是，$A$ 和 $B$ 是随机变量，它们的平方和 $A^2+B^2$ **是一个随机变量**，并不恒等于常数 $2\sigma^2$（除非 $A, B$ 是常数，但题目说它们是方差为 $\sigma^2$ 的随机变量）。

也就是说，对于不同的样本（即不同的实验结果，$A, B$ 取不同值），算出来的时间平均自相关函数是不一样的，并不收敛这就说明它**不具有**各态历经性。

**结论：该过程不具有自相关函数的各态历经性。**

## 13 

这道题和上一题（第12题）非常像，但有一个**微妙且关键的区别**。

上一题明确说了 $A, B$ 是**正态分布**，而这一题只说了是**随机变量**。这给了我们更多的讨论空间，因为“各态历经性”成立与否，取决于 $A$ 和 $B$ 的具体分布性质。

首先，题目中你漏写了 $D(B)$，但为了使讨论有意义（即让过程成为宽平稳过程），我们必须假设题目隐含了 **$D(B) = D(A) = \sigma^2$**。

以下是详细解答：

---

### 第一步：计算统计特性（Ensemble Averages）

我们需要先算出“理论值”，作为判断的标准。

1.  **均值** $m_X(t)$：
$$E[X(t)] = E[A]\cos t + E[B]\sin t = 0$$
（因为 $E[A]=E[B]=0$）

2.  **自相关函数** $R_X(t, t+\tau)$：
$$
R_X(t, t+\tau) = E[(A\cos t + B\sin t)(A\cos(t+\tau) + B\sin(t+\tau))]
$$
展开后，利用 $A, B$ 相互独立（$E[AB]=0$）且方差相等（$E[A^2]=E[B^2]=\sigma^2$）：
$$
= \sigma^2 \cos t \cos(t+\tau) + \sigma^2 \sin t \sin(t+\tau)
$$
利用三角恒等式：
$$
R_X(\tau) = \sigma^2 \cos \tau
$$

**结论**：这是一个宽平稳过程。我们接下来的任务是验证时间平均是否等于上面的 $0$ 和 $\sigma^2 \cos \tau$。

---

### 第二步：讨论均值的各态历经性

计算样本函数 $x(t) = a\cos t + b\sin t$ 的时间平均：

$$
\langle x(t) \rangle = \lim_{T \to \infty} \frac{1}{2T} \int_{-T}^{T} (a\cos t + b\sin t) dt
$$

由于 $\cos t$ 和 $\sin t$ 在无穷区间上的平均值为 0：
$$
\langle x(t) \rangle = 0
$$

**对比**：
* 时间平均 $= 0$
* 统计均值 $= 0$

**结论：** 无论 $A, B$ 服从什么分布，该过程**具有均值的各态历经性**。

---

### 第三步：讨论自相关函数的各态历经性

计算样本函数的时间自相关 $\mathcal{R}_X(\tau)$：

$$
\mathcal{R}_X(\tau) = \langle x(t)x(t+\tau) \rangle
$$

展开被积函数（和上一题一样的技巧，利用积化和差）：
$$
\begin{aligned}
x(t)x(t+\tau) &= [a\cos t + b\sin t][a\cos(t+\tau) + b\sin(t+\tau)] \\
\text{时间平均} &= a^2 \langle \cos t \cos(t+\tau) \rangle + b^2 \langle \sin t \sin(t+\tau) \rangle + ab \langle \dots \rangle
\end{aligned}
$$

利用时间平均性质：
* $\langle \cos t \cos(t+\tau) \rangle = \frac{1}{2}\cos \tau$
* $\langle \sin t \sin(t+\tau) \rangle = \frac{1}{2}\cos \tau$
* 交叉项平均为 0

得到**时间自相关函数**：
$$
\mathcal{R}_X(\tau) = \frac{A^2 + B^2}{2} \cos \tau
$$
*(注意：这里 $A, B$ 对应具体的样本观测值)*

**对比统计自相关函数**：
$$
R_X(\tau) = \sigma^2 \cos \tau
$$

**核心讨论：**
要使过程具有各态历经性，必须满足：
$$
\mathcal{R}_X(\tau) = R_X(\tau) \quad \text{(以概率 1 成立)}
$$
即要求：
$$
\frac{A^2 + B^2}{2} = \sigma^2 \implies A^2 + B^2 = 2\sigma^2
$$

这里就是第12题和第13题的区别所在：

1.  **一般情况（大部分随机变量）**：
如果 $A, B$ 是像正态分布、均匀分布这样的连续随机变量，那么 $A^2 + B^2$ 是一个**随机变量**（会波动的），它不可能恒等于常数 $2\sigma^2$。
* **结论**：在一般情况下，**不具有**自相关函数的各态历经性。

2.  **特殊情况（离散分布）**：
如果 $A$ 和 $B$ 是离散随机变量，且只取值 $+\sigma$ 或 $-\sigma$（概率各为0.5）。
* 此时 $A^2 \equiv \sigma^2$， $B^2 \equiv \sigma^2$。
* 那么 $A^2 + B^2 = 2\sigma^2$ **恒成立**。
* **结论**：在这种特殊的“二值分布”下，它**具有**自相关函数的各态历经性。

---

### 考试应该怎么写？

既然题目只说了“随机变量”而没指定分布，最严谨的答案是**分类讨论**或**指出条件**：

**答：**
1.  **均值各态历经性**：
时间平均 $\langle x(t) \rangle = 0$，统计平均 $E[X(t)] = 0$，两者相等，故**具有均值各态历经性**。

2.  **自相关各态历经性**：
统计自相关为 $R_X(\tau) = \sigma^2 \cos \tau$。
时间自相关为 $\mathcal{R}_X(\tau) = \frac{A^2+B^2}{2} \cos \tau$。
* 若随机变量 $A, B$ 满足 $A^2 + B^2 = 2\sigma^2$ (例如 $P(A=\pm \sigma)=0.5$)，则具有各态历经性。
* 若 $A, B$ 为一般随机变量（如正态分布），$A^2+B^2$ 不恒等于 $2\sigma^2$，则**不具有**自相关各态历经性。


## 14

### 第一步：计算统计平均（理论值）

我们需要先算出“上帝视角”下的统计均值和自相关函数。

#### 1. 统计均值 $m_X(t)$
$$
E[X(t)] = E[A \cos(\omega t + \Theta)]
$$
由于 $A$ 和 $\Theta$ 相互独立，期望可以拆开：
$$
= E[A] \cdot E[\cos(\omega t + \Theta)]
$$
重点看后一项，已知 $\Theta \sim U[0, 2\pi]$，其概率密度为 $\frac{1}{2\pi}$：
$$
E[\cos(\omega t + \Theta)] = \int_{0}^{2\pi} \cos(\omega t + \theta) \frac{1}{2\pi} d\theta = 0
$$
（在一个完整周期内积分余弦函数，结果必为 0）
**所以：**
$$
m_X = 0
$$

#### 2. 统计自相关函数 $R_X(\tau)$
$$
R_X(t, t+\tau) = E[X(t)X(t+\tau)]
$$
$$
= E[A^2 \cos(\omega t + \Theta) \cos(\omega (t+\tau) + \Theta)]
$$
利用独立性，$E[A^2]$ 和角度部分的期望分开算。
利用积化和差公式 $\cos \alpha \cos \beta = \frac{1}{2}[\cos(\alpha-\beta) + \cos(\alpha+\beta)]$：
$$
= E[A^2] \cdot E\left[ \frac{1}{2} \cos(\omega \tau) + \frac{1}{2} \cos(2\omega t + \omega \tau + 2\Theta) \right]
$$
* 第一项 $\frac{1}{2} \cos(\omega \tau)$ 是常数（相对于期望而言）。
* 第二项含有 $2\Theta$，在 $[0, 2\pi]$ 上积分依然为 0。

所以：
$$
R_X(\tau) = \frac{1}{2} E[A^2] \cos(\omega \tau)
$$

**关键点：计算 $E[A^2]$**
题目给出了 $A$ 的概率密度是瑞利分布：$f(x) = \frac{x}{\sigma^2} e^{-\frac{x^2}{2\sigma^2}}$。
我们可以直接利用瑞利分布的性质，或者积分计算二阶矩：
$$
E[A^2] = \int_{0}^{\infty} x^2 \cdot \frac{x}{\sigma^2} e^{-\frac{x^2}{2\sigma^2}} dx
$$
令 $u = \frac{x^2}{2\sigma^2}$，则 $x^2 = 2\sigma^2 u$，且 $dx = \frac{\sigma^2}{x} du$。
代换后这个积分结果为 **$2\sigma^2$**。（这是瑞利分布的一个重要特征：能量 $E[A^2] = 2\sigma^2$）。

代回原式：
$$
R_X(\tau) = \frac{1}{2} (2\sigma^2) \cos(\omega \tau) = \sigma^2 \cos(\omega \tau)
$$

---

### 第二步：计算时间平均（观测值）

#### 1. 时间均值 $\langle x(t) \rangle$
对于任意样本 $x(t) = a \cos(\omega t + \theta)$：
$$
\langle x(t) \rangle = \lim_{T \to \infty} \frac{1}{2T} \int_{-T}^{T} a \cos(\omega t + \theta) dt = 0
$$
（余弦波的直流分量为 0）

#### 2. 时间自相关函数 $\mathcal{R}_X(\tau)$
$$
\mathcal{R}_X(\tau) = \langle x(t)x(t+\tau) \rangle
$$
利用积化和差及时间平均的性质（高频分量平均为0）：
$$
\begin{aligned}
\langle a \cos(\omega t + \theta) \cdot a \cos(\omega t + \omega \tau + \theta) \rangle &= \frac{a^2}{2} \langle \cos(\omega \tau) + \cos(2\omega t + \dots) \rangle \\
&= \frac{a^2}{2} \cos(\omega \tau)
\end{aligned}
$$
写成随机变量形式：
$$
\mathcal{R}_X(\tau) = \frac{A^2}{2} \cos(\omega \tau)
$$

---

### 第三步：讨论各态历经性（对比）

#### 1. 均值各态历经性
* 统计平均 $m_X = 0$
* 时间平均 $\langle x(t) \rangle = 0$
* **结论：** 两者相等，**具有**均值各态历经性。

#### 2. 自相关各态历经性
* 统计自相关 $R_X(\tau) = \sigma^2 \cos(\omega \tau)$
* 时间自相关 $\mathcal{R}_X(\tau) = \frac{A^2}{2} \cos(\omega \tau)$

**分析：**
要让两者相等，必须要求随机变量 $A$ 满足：
$$
\frac{A^2}{2} = \sigma^2 \implies A^2 = 2\sigma^2 \implies A = \sqrt{2}\sigma \quad (\text{常数})
$$
但是，题目明确指出 **$A$ 服从瑞利分布**。这意味着 $A$ 是一个会在不同样本间变化的随机变量，它不可能恒等于常数 $\sqrt{2}\sigma$。
因此，时间平均 $\neq$ 统计平均。

* **结论：** 该过程**不具有**自相关函数的各态历经性。

---

### 💡 总结与直观理解


考试时，只要算出 $R_X = \sigma^2 \cos(\omega \tau)$ 和 $\mathcal{R}_X = \frac{A^2}{2} \cos(\omega \tau)$，然后指出 **“因为 $A$ 是瑞利分布变量，$\frac{A^2}{2} \neq \sigma^2$”**，这道题就拿满分了。

## 18

**解题思路：**
对于线性时不变系统（LTI），如果输入是平稳过程，我们通常采用**频域法（功率谱密度法）**来求解输出的自相关函数，因为在频域进行乘法运算比在时域进行卷积运算（特别是双重卷积）要简单得多。

以下是详细步骤：

---

### 第一步：确定系统的性质

首先，我们需要求出系统的频率响应函数 $H(\omega)$。
对微分方程 $Y'(t) + \alpha Y(t) = \alpha X(t)$ 两边同时做傅里叶变换：

$$
j\omega Y(\omega) + \alpha Y(\omega) = \alpha X(\omega)
$$

整理得到传输函数：
$$
H(\omega) = \frac{Y(\omega)}{X(\omega)} = \frac{\alpha}{\alpha + j\omega}
$$

这就对应模的平方（功率传输系数）：
$$
|H(\omega)|^2 = H(\omega)H^*(\omega) = \frac{\alpha}{\alpha + j\omega} \cdot \frac{\alpha}{\alpha - j\omega} = \frac{\alpha^2}{\alpha^2 + \omega^2}
$$

---

### 第二步：求输出的均值函数 $m_Y$

对于线性系统，输出的均值等于输入的均值乘以系统的直流增益 $H(0)$。

1.  **系统直流增益**：令 $\omega = 0$，则 $H(0) = \frac{\alpha}{\alpha + 0} = 1$。
2.  **输入均值**：已知 $m_X = 0$。

因此，输出均值为：
$$
m_Y = m_X \cdot H(0) = 0 \cdot 1 = 0
$$

**结论：** 输出均值函数 $m_Y(t) = 0$。

---

### 第三步：求输出的相关函数 $R_Y(\tau)$

我们利用功率谱密度（PSD）来求解。根据维纳-辛钦定理（Wiener-Khinchin Theorem），相关函数和功率谱密度是一对傅里叶变换对。

**1. 求输入的功率谱密度 $S_X(\omega)$**
已知 $R_X(\tau) = \sigma^2 e^{-\beta |\tau|}$，利用常用傅里叶变换对 $e^{-a|t|} \leftrightarrow \frac{2a}{a^2+\omega^2}$：

$$
S_X(\omega) = \mathcal{F}\{ R_X(\tau) \} = \sigma^2 \frac{2\beta}{\beta^2 + \omega^2}
$$

**2. 求输出的功率谱密度 $S_Y(\omega)$**
根据公式 $S_Y(\omega) = |H(\omega)|^2 S_X(\omega)$：

$$
S_Y(\omega) = \frac{\alpha^2}{\alpha^2 + \omega^2} \cdot \frac{2\sigma^2 \beta}{\beta^2 + \omega^2}
$$
整理得：
$$
S_Y(\omega) = 2\sigma^2 \alpha^2 \beta \cdot \frac{1}{(\omega^2 + \alpha^2)(\omega^2 + \beta^2)}
$$

**3. 进行部分分式展开（Partial Fraction Decomposition）**
为了方便做傅里叶逆变换，我们需要把分母拆开。
设 $\frac{1}{(\omega^2 + \alpha^2)(\omega^2 + \beta^2)} = \frac{A}{\omega^2 + \alpha^2} + \frac{B}{\omega^2 + \beta^2}$。

通分后分子为 $A(\omega^2 + \beta^2) + B(\omega^2 + \alpha^2) = 1$。
* 令 $\omega^2 = -\alpha^2 \implies A(\beta^2 - \alpha^2) = 1 \implies A = \frac{1}{\beta^2 - \alpha^2}$
* 令 $\omega^2 = -\beta^2 \implies B(-\beta^2 + \alpha^2) = 1 \implies B = \frac{-1}{\beta^2 - \alpha^2}$

代回 $S_Y(\omega)$ 表达式：
$$
S_Y(\omega) = \frac{2\sigma^2 \alpha^2 \beta}{\beta^2 - \alpha^2} \left( \frac{1}{\omega^2 + \alpha^2} - \frac{1}{\omega^2 + \beta^2} \right)
$$

**4. 求傅里叶逆变换得到 $R_Y(\tau)$**
我们需要将式子凑成标准形式 $\frac{2a}{a^2+\omega^2}$ 以便直接逆变换。

$$
S_Y(\omega) = \frac{\sigma^2 \alpha^2 \beta}{\beta^2 - \alpha^2} \left( \frac{1}{\alpha} \cdot \frac{2\alpha}{\omega^2 + \alpha^2} - \frac{1}{\beta} \cdot \frac{2\beta}{\omega^2 + \beta^2} \right)
$$

现在分别对两项进行逆变换：
* $\mathcal{F}^{-1} \{ \frac{2\alpha}{\omega^2 + \alpha^2} \} = e^{-\alpha |\tau|}$
* $\mathcal{F}^{-1} \{ \frac{2\beta}{\omega^2 + \beta^2} \} = e^{-\beta |\tau|}$

带入系数计算：
$$
\begin{aligned}
R_Y(\tau) &= \frac{\sigma^2 \alpha^2 \beta}{\beta^2 - \alpha^2} \left( \frac{1}{\alpha} e^{-\alpha |\tau|} - \frac{1}{\beta} e^{-\beta |\tau|} \right) \\
&= \frac{\sigma^2 \alpha^2 \beta}{\beta^2 - \alpha^2} \cdot \frac{1}{\alpha} e^{-\alpha |\tau|} - \frac{\sigma^2 \alpha^2 \beta}{\beta^2 - \alpha^2} \cdot \frac{1}{\beta} e^{-\beta |\tau|} \\
&= \frac{\sigma^2 \alpha \beta}{\beta^2 - \alpha^2} e^{-\alpha |\tau|} - \frac{\sigma^2 \alpha^2}{\beta^2 - \alpha^2} e^{-\beta |\tau|}
\end{aligned}
$$

提取公因式 $\frac{\sigma^2 \alpha}{\beta^2 - \alpha^2}$，得到最终结果：

$$
R_Y(\tau) = \frac{\sigma^2 \alpha}{\beta^2 - \alpha^2} \left( \beta e^{-\alpha |\tau|} - \alpha e^{-\beta |\tau|} \right)
$$

---

### 总结答案

1.  **均值函数**：
$$m_Y(t) = 0$$

2.  **相关函数**：
$$R_Y(\tau) = \frac{\sigma^2 \alpha}{\beta^2 - \alpha^2} \left( \beta e^{-\alpha |\tau|} - \alpha e^{-\beta |\tau|} \right)$$

**(检查提示)**：注意到题目条件 $\alpha \neq \beta$，这保证了分母不为零。如果 $\alpha = \beta$，则需要对传递函数取极限或使用重根的逆变换方法，结果会包含 $t \cdot e^{-t}$ 项。

## 21

这是一道证明**均值各态历经性（Mean Ergodicity）**的标准题目。

证明的思路非常清晰：我们需要验证该过程的时间平均的方差在观测时间趋于无穷大时是否趋于 0。

以下是标准的证明步骤：

### 第一步：确定协方差函数 $C_X(\tau)$

对于平稳过程，协方差函数定义为：
$$
C_X(\tau) = R_X(\tau) - m_X^2
$$
已知题目条件：
* $m_X = 0$
* $R_X(\tau) = \sigma^2 e^{-\alpha |\tau|}$

因此：
$$
C_X(\tau) = \sigma^2 e^{-\alpha |\tau|} - 0^2 = \sigma^2 e^{-\alpha |\tau|}
$$

### 第二步：利用均值各态历经性的充分条件

**定理（均值各态历经的充分条件）：**
如果平稳过程的协方差函数 $C_X(\tau)$ 满足以下条件：
$$
\lim_{T \to \infty} \frac{1}{2T} \int_{-2T}^{2T} C_X(\tau) d\tau = 0
$$
或者更严格的（Slutsky 定理）：
$$
\lim_{T \to \infty} \frac{1}{T} \int_{0}^{2T} \left(1 - \frac{\tau}{2T}\right) C_X(\tau) d\tau = 0
$$
则该过程具有均值各态历经性。

通常在考试中，使用第一个较宽松的充分条件（积分平均值为0）或者**绝对可积性**就足够了。

**方法一：证明绝对可积（最快的方法）**
如果 $\int_{-\infty}^{+\infty} |C_X(\tau)| d\tau < \infty$，则均值各态历经。
$$
\int_{-\infty}^{+\infty} \sigma^2 e^{-\alpha |\tau|} d\tau = 2 \int_{0}^{+\infty} \sigma^2 e^{-\alpha \tau} d\tau = 2\sigma^2 \left[ -\frac{1}{\alpha} e^{-\alpha \tau} \right]_0^{+\infty} = \frac{2\sigma^2}{\alpha}
$$
因为 $\sigma^2$ 和 $\alpha$ 都是常数，这个积分结果是一个有限值。
**结论：** 满足充分条件，故具有均值各态历经性。

---

**方法二：利用极限定义推导（更严谨的考试写法）**

我们要考察当 $T \to \infty$ 时，时间平均的方差是否趋于 0。考察积分项：

$$
J(T) = \frac{1}{2T} \int_{-2T}^{2T} C_X(\tau) d\tau
$$

1.  **代入 $C_X(\tau)$**：
利用偶函数性质，将积分区间改为 $[0, 2T]$：
$$
\int_{-2T}^{2T} \sigma^2 e^{-\alpha |\tau|} d\tau = 2 \int_{0}^{2T} \sigma^2 e^{-\alpha \tau} d\tau
$$

2.  **计算定积分**：
$$
= 2\sigma^2 \left[ -\frac{1}{\alpha} e^{-\alpha \tau} \right]_{0}^{2T} = \frac{2\sigma^2}{\alpha} (1 - e^{-2\alpha T})
$$

3.  **计算极限**：
将上述结果代回 $J(T)$：
$$
\lim_{T \to \infty} J(T) = \lim_{T \to \infty} \frac{1}{2T} \cdot \frac{2\sigma^2}{\alpha} (1 - e^{-2\alpha T})
$$
整理常数项：
$$
= \lim_{T \to \infty} \frac{\sigma^2}{\alpha T} (1 - e^{-2\alpha T})
$$
* 当 $T \to \infty$ 时，$\frac{1}{T} \to 0$。
* 当 $T \to \infty$ 时，$(1 - e^{-2\alpha T}) \to 1$。

因此：
$$
\lim_{T \to \infty} J(T) = 0 \cdot 1 = 0
$$

### 结论
由于满足 $\lim_{T \to \infty} \frac{1}{2T} \int_{-2T}^{2T} C_X(\tau) d\tau = 0$，故该平稳过程 $\{X(t)\}$ 的均值具有各态历经性。

